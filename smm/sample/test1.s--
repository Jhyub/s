(* was:
   fn (x, y, z) =>
       let f := fn (x, y) => x + y in
       if f(x, y) = 4 then z else 0
   run with x=3, y=1, z=99 *)
let x := 3 in
let y := 1 in
let z := 99 in
let fn f(x, y) => x + y in
if f(x, y) = 4 then z else 0